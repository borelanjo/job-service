version: '3'

services:

  #############################################################
  ######################### DATABASES #########################
  #############################################################

  db-job:
    container_name: db-job
    image: postgres:9.6
    restart: always
    hostname: db-job
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: job1
      POSTGRES_USER: job1
      POSTGRES_PASSWORD: job1
      PGDATA: /tmp
    volumes:
      - ./docker/postgres/data-despesa:/var/lib/postgresql/data